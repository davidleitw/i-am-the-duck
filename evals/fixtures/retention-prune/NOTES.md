# 保留清理操作筆記

1. 每次清理至少保留 `keep_last` 份可用備份，最新一份完整備份永遠不會被刪。
2. 釘選（pinned）或帶 `legal-hold` 標籤的快照，任何情況下都不會被清理。
3. 未完成（partial）的快照超過寬限期後，會在下一次清理時移除。
4. 清理中途程式中斷，重啟後會自動把沒做完的刪除收尾。

此範例使用本機 SQLite 與資料移除紀錄，不連接真實儲存後端。
